{
  // Auto-generated by CP Folder Helper. Safe to edit; we won't overwrite it.
  // Requires the CodeLLDB extension (vadimcn.vscode-lldb) for C++ debugging.
  "version": "0.2.0",
  "configurations": [
    {
      "name": "CP: Debug C++",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "stdio": null,
      "terminal": "integrated",
      "preLaunchTask": "CP: Build (g++ -O2)"
    },
    {
      "name": "CP: Debug C++ (with sample 1 input)",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "stdio": ["${fileDirname}/_tests/in-1.txt", null, null],
      "terminal": "integrated",
      "preLaunchTask": "CP: Build (g++ -O2)"
    }
  ]
}
